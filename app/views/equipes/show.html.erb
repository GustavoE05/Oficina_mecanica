<p style="color: green"><%= notice %></p>

<%= render @equipe %>

<div class="mt-5">
<div class="mt-1" >
  <%= link_to "Editar", edit_equipe_path(@equipe), class:"btn btn-primary"%> 
</div>
<div class="mt-1" >
  <%= link_to "Voltar", equipes_path, class:"btn btn-warning"%>
</div>
<div class="mt-1" >
  <%= button_to "Excluir", @equipe, method: :delete, class: "btn btn-danger"%>
</div>
</div>

